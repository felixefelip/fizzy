# ⚠️  Generated by rbs_infer for debugging — NEVER loaded at runtime.
#     Expanded view of app/controllers/boards/publications_controller.rb (macros desugared into pseudo-code).
#     Regenerated on every run; do not edit.
class Boards::PublicationsController < ApplicationController
  include BoardScoped

  before_action :ensure_permission_to_admin_board

  def create
    @board.publish

    respond_to do |format|
      format.turbo_stream
      format.json { render partial: "boards/board", locals: { board: @board }, status: :created }
    end
  end

  def destroy
    @board.unpublish
    @board.reload

    respond_to do |format|
      format.turbo_stream
      format.json { head :no_content }
    end
  end
end

class Boards::PublicationsController
  before_action :set_board
end
