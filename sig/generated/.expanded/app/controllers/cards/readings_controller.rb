# ⚠️  Generated by rbs_infer for debugging — NEVER loaded at runtime.
#     Expanded view of app/controllers/cards/readings_controller.rb (macros desugared into pseudo-code).
#     Regenerated on every run; do not edit.
class Cards::ReadingsController < ApplicationController
  include CardScoped

  def create
    @notification = @card.read_by(Current.user)
    record_board_access

    respond_to do |format|
      format.turbo_stream
      format.json { head :created }
    end
  end

  def destroy
    @notification = @card.unread_by(Current.user)
    record_board_access

    respond_to do |format|
      format.turbo_stream
      format.json { head :no_content }
    end
  end

  private
    def record_board_access
      @card.board.accessed_by(Current.user)
    end
end

class Cards::ReadingsController
  before_action :set_card, :set_board
end
