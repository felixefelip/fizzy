# ⚠️  Generated by rbs_infer for debugging — NEVER loaded at runtime.
#     Expanded view of app/controllers/webhooks/deliveries_controller.rb (macros desugared into pseudo-code).
#     Regenerated on every run; do not edit.
class Webhooks::DeliveriesController < ApplicationController
  include BoardScoped

  before_action :ensure_admin
  before_action :set_webhook

  def index
    respond_to do |format|
      format.json do
        set_page_and_extract_portion_from @webhook.deliveries.ordered.includes(event: [ :creator, { eventable: :card } ])
      end
    end
  end

  private
    def set_webhook
      @webhook = @board.webhooks.find(params[:webhook_id])
    end
end

class Webhooks::DeliveriesController
  before_action :set_board
end
