# ⚠️  Generated by rbs_infer for debugging — NEVER loaded at runtime.
#     Expanded view of lib/action_pack/web_authn/current.rb (macros desugared into pseudo-code).
#     Regenerated on every run; do not edit.
# = Action Pack WebAuthn Current Attributes
#
# Thread-isolated request-scoped attributes for WebAuthn ceremonies. These are
# set automatically by ActionPack::Passkey::Request at the start of each
# request and consumed by the registration/authentication flows.
#
# == Attributes
#
# [+host+]
#   The relying party identifier (typically +request.host+). Used as the
#   default RelyingParty ID.
#
# [+origin+]
#   The expected origin (typically +request.base_url+). Validated against the
#   +origin+ field in the authenticator's client data.
#
class ActionPack::WebAuthn::Current < ActiveSupport::CurrentAttributes
  module GeneratedAttributeMethods
    def host
      @host
    end
    def host=(value)
      @host = value
    end
    def origin
      @origin
    end
    def origin=(value)
      @origin = value
    end
  end
  include GeneratedAttributeMethods

  def self.host
    @host
  end

  def self.host=(value)
    @host = value
  end

  def self.origin
    @origin
  end

  def self.origin=(value)
    @origin = value
  end

  def self.set(host: nil, origin: nil, &block)
    @host = host
    @origin = origin
    block.call
  end

  def self.with(host: nil, origin: nil, &block)
    @host = host
    @origin = origin
    block.call
  end
end
