# ⚠️  Generated by rbs_infer for debugging — NEVER loaded at runtime.
#     Expanded view of lib/rails_ext/action_pack_passkey_infer_name_from_aaguid.rb (macros desugared into pseudo-code).
#     Regenerated on every run; do not edit.
module ActionPackPasskeyInferNameFromAaguid
  extend ActiveSupport::Concern

  module ClassMethods
def register(...)
      super(...).tap do |credential|
        credential.update!(name: credential.authenticator.name) if credential.authenticator && credential.name.blank?
      end
    end
end

  def authenticator
    Passkey::Authenticator.find_by_aaguid(aaguid)
  end
end
