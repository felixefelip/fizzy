# frozen_string_literal: true
#
# GENERATED by RbsInfer::Extensions::Rails::Controllers::RuntimeGenerator.
# Regenerated on every run; do not edit.

class CardsController
  def render(target = nil, *rest)
    @__rbs_infer__performed = true
    case target
    when :edit then ERBCardsEdit.new(card: @card).__rbs_infer__body
    when :index then ERBCardsIndex.new(filter: @filter, page: @page, user_filtering: @user_filtering).__rbs_infer__body
    when :show then ERBCardsShow.new(card: @card).__rbs_infer__body
    when :update then ERBCardsUpdate.new(card: @card).__rbs_infer__body
    end
    true
  end

  private

  def __rbs_infer__run_index
    require_account
    return if performed?

    require_authentication
    return if performed?

    ensure_can_access_account if authenticated_account_access?
    return if performed?

    Current.http_method = request.method
    Current.request_id  = request.uuid
    Current.user_agent  = request.user_agent
    Current.ip_address  = request.ip
    Current.referrer    = request.referrer
    return if performed?

    disable_view_transitions if page_refresh?
    return if performed?

    set_target_header
    return if performed?

    set_filter
    return if performed?

    set_user_filtering
    return if performed?

    index

    return if performed?

    render :index
  end

  def __rbs_infer__run_create
    require_account
    return if performed?

    require_authentication
    return if performed?

    ensure_can_access_account if authenticated_account_access?
    return if performed?

    Current.http_method = request.method
    Current.request_id  = request.uuid
    Current.user_agent  = request.user_agent
    Current.ip_address  = request.ip
    Current.referrer    = request.referrer
    return if performed?

    disable_view_transitions if page_refresh?
    return if performed?

    set_target_header
    return if performed?

    set_filter
    return if performed?

    set_user_filtering
    return if performed?

    set_board
    return if performed?

    create
  end

  def __rbs_infer__run_show
    require_account
    return if performed?

    require_authentication
    return if performed?

    ensure_can_access_account if authenticated_account_access?
    return if performed?

    Current.http_method = request.method
    Current.request_id  = request.uuid
    Current.user_agent  = request.user_agent
    Current.ip_address  = request.ip
    Current.referrer    = request.referrer
    return if performed?

    disable_view_transitions if page_refresh?
    return if performed?

    set_target_header
    return if performed?

    set_filter
    return if performed?

    set_user_filtering
    return if performed?

    set_card
    return if performed?

    redirect_if_drafted
    return if performed?

    show

    return if performed?

    render :show
  end

  def __rbs_infer__run_edit
    require_account
    return if performed?

    require_authentication
    return if performed?

    ensure_can_access_account if authenticated_account_access?
    return if performed?

    Current.http_method = request.method
    Current.request_id  = request.uuid
    Current.user_agent  = request.user_agent
    Current.ip_address  = request.ip
    Current.referrer    = request.referrer
    return if performed?

    disable_view_transitions if page_refresh?
    return if performed?

    set_target_header
    return if performed?

    set_filter
    return if performed?

    set_user_filtering
    return if performed?

    set_card
    return if performed?

    edit

    return if performed?

    render :edit
  end

  def __rbs_infer__run_update
    require_account
    return if performed?

    require_authentication
    return if performed?

    ensure_can_access_account if authenticated_account_access?
    return if performed?

    Current.http_method = request.method
    Current.request_id  = request.uuid
    Current.user_agent  = request.user_agent
    Current.ip_address  = request.ip
    Current.referrer    = request.referrer
    return if performed?

    disable_view_transitions if page_refresh?
    return if performed?

    set_target_header
    return if performed?

    set_filter
    return if performed?

    set_user_filtering
    return if performed?

    set_card
    return if performed?

    update

    return if performed?

    render :update
  end

  def __rbs_infer__run_destroy
    require_account
    return if performed?

    require_authentication
    return if performed?

    ensure_can_access_account if authenticated_account_access?
    return if performed?

    Current.http_method = request.method
    Current.request_id  = request.uuid
    Current.user_agent  = request.user_agent
    Current.ip_address  = request.ip
    Current.referrer    = request.referrer
    return if performed?

    disable_view_transitions if page_refresh?
    return if performed?

    set_target_header
    return if performed?

    set_filter
    return if performed?

    set_user_filtering
    return if performed?

    set_card
    return if performed?

    ensure_permission_to_administer_card
    return if performed?

    destroy
  end
end
